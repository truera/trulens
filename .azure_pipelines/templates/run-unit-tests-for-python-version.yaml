parameters:
  - name: pythonVersion
    type: string
    values:
      - "3.9"
      - "3.10"
      - "3.11"
      - "3.12"

jobs:
- job: RunAllUnitTestsForPython${{ replace(parameters.pythonVersion, '.', '') }}
  pool:
    vmImage: ubuntu-latest
  timeoutInMinutes: 30
  steps:
  - template: env-setup.yaml
  - template: run-precommits.yaml
  - template: run-tests.yaml
    parameters:
      pytestMarker: basic
      testSuite: unit
  - template: run-tests.yaml
    parameters:
      pytestMarker: optional
      testSuite: unit
  - template: run-tests.yaml
    parameters:
      pytestMarker: snowflake
      testSuite: unit
